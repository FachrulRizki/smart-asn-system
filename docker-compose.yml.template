version: "3.8"
services:
  backend:
    image: my-backend:latest
    env_file:
      - .env
    ports:
      - "8000:8000"
  ai-service:
    image: my-ai-service:latest
    env_file:
      - .env
    ports:
      - "9000:9000"
